Put the STP root directory here. 

./clean-install.sh --with-g++='g++ -fPIC' --with-gcc='gcc -fPIC' --with-prefix=$PWD/../install

We compile it with -fPIC because we are going to link it with .so files. 
